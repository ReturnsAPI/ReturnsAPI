- Use debug.getlocal on arg1 of each method; if it's "namespace", then bind the mod namespace in a closure
    - Also check last arg; if that is "namespace", then allow specifying ns optionally

- Mace Replica item log seems to be in the wrong group, so just move it in initialize