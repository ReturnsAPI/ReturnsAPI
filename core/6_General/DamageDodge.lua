-- TODO

-- Midhook `damage_get_dodge` after line 20
-- Allow the user to force trigger a dodge with any damage popup word (IMMUNE, INVINCIBLE, BLOCK, or PARRY)
    -- In each callback function, should inform them if a dodge has already happened (so multiple effects don't get consumed at once)